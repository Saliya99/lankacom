import React, { useState } from "react";

export default function userForm({ onAdd }) {

  const [name, setName] = useSate('');
  const [email, setEmail] = useSate('');
  const [formError, setFormError] = useSate('');


  const handleSubmit = (e) => {
    e.prevernt

  }

} 